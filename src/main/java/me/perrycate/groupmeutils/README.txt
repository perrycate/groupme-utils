me.perrycate.groupmeutils

Contains some helpful classes for doing useful tasks with the groupme API (for
example dumping a group to a text file), and all of the subpackages required to
make them work.
